<section class="content-block py-3">
  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <img src="{{post.get_field.home_content_block.image.src}}" alt="{{post.get_field.home_content_block.image.alt}}" width="{{post.get_field.home_content_block.image.width}}" height="{{post.get_field.home_content_block.image.height}}">
      </div>
      <div class="col-md-6">
        <header>
          <h2>{{post.get_field.home_content_block.title}}</h2>
        </header>
        <p>{{post.get_field.home_content_block.content}}</p>
      </div>
    </div>
  </div>
</section>
