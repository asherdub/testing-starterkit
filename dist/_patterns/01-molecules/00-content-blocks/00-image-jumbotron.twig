<div class="d-flex text-white jumbotron image-jumbotron jumbotron-fluid p-0">
  <div class="image-wrap">
    <img class="jumbotron-image" src="{{post.get_field.jumbotron.image.src}}" alt="{{post.get_field.jumbotron.image.src}}">
  </div>
  <div class="align-self-center container">
    <h1 class="display-3">{{post.get_field.jumbotron.title}}</h1>
    <p class="lead">{{post.get_field.jumbotron.message}}</p>
    <a class="btn btn-primary btn-lg" href="{{post.get_field.jumbotron.link}}" role="button">{{post.get_field.jumbotron.link_label}}</a>
  </div>
</div>
